{
  "name": "@jmpp/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "prisma"
  ],
  "scripts": {
    "lint": "echo 'Skipping lint for db package'",
    "lint:fix": "echo 'Skipping lint:fix for db package'",
    "type-check": "echo 'Skipping type-check for db package'",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset --force"
  },
  "dependencies": {
    "@jmpp/types": "workspace:*",
    "@prisma/client": "^5.15.0"
  },
  "devDependencies": {
    "@jmpp/config": "workspace:*",
    "prisma": "^5.15.0",
    "typescript": "^5.5.3",
    "tsx": "^4.16.2"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
